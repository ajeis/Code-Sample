@model CourseRegistration44.ViewModel.CoursesViewModel

@{
    ViewBag.Title = "Courses";
}

<h2>Courses</h2>
<a href="http://www.google.com">Link</a>

@section JQueryScript
{
   @* <script type="text/javascript">
        $(document).ready(function () {
            $("a").click(function () {
                alert("Hello world!");
            });
        });
    </script>*@

    <link rel="stylesheet" type="text/css" media="screen" href="/Content/jquery.jqGrid/smoothness/jquery-ui-1.8.23.custom.css" />
    <link rel="stylesheet" type="text/css" media="screen" href="/Content/jquery.jqGrid/ui.jqgrid.css" />
    <script src="@Url.Content("~/Scripts/i18n/grid.locale-en.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.jqGrid.src.js")" type="text/javascript"></script>


    <script src="@Url.Content("~/Scripts/jqGrid.Course.js")" type="text/javascript"></script>
}

<button id="refreshButton">Refresh</button>

<h2>My Registered Courses</h2>
<table id="listTableRegistered" class="scroll"></table>
<div id="pagerRegistered" class="scroll" style="text-align: center;"></div>


<h2>My NON registered Courses</h2>
<table id="listTableNonRegistered" class="scroll"></table>
<div id="pager" class="scroll" style="text-align: center;"></div>

<a href="#popupRegister" id="regButton" data-rel="popup" data-position-to="window" 
    data-role="button" data-inline="true" onclick="">Register</a> Select only one course at a time
@*<Button id="regButton" data-rel="popup" data-position-to="window" 
    data-role="button" data-inline="true">Register</Button> Select only one course at a time*@

<fieldset>
    <legend>CoursesViewModel</legend>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { /* id=Model.PrimaryKey */ }) |
    @Html.ActionLink("Back to List", "Index")
</p>

<div data-role="popup" id="popupRegister" data-theme="a"  style="display:none">
			<form>
				<div style="padding:10px 20px;">
				  <h3>Add optional information</h3>
		          <label for="un" class="ui-hidden-accessible">Notes(previoud experience, etc):</label>
		          <input type="text" name="notes" id="notes" value="azxs"  data-theme="a" />

		          <label for="un" class="ui-hidden-accessible">Upload Resume</label>
		          <input type="text" name="resume" id="resume" value="qq" data-theme="a" />

		    	  <button data-theme="b" id="popupSub">Submit</button>
				</div>
			</form>
		</div>